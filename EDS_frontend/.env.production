# Production Environment
# Only used by `npm run build` invoked directly (not the Docker path —
# docker-compose.yml passes VITE_API_BASE_URL from the root .env as a build
# arg instead). Leave empty to call the API same-origin via nginx.
VITE_API_BASE_URL=https://eds.dabdrt.com